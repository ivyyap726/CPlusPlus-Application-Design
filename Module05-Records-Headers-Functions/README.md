# Module 05 - Records, Headers & Functions

Added a loadRecords() function that reads the transaction data from personal_finance_data.csv at startup, a working addRecord() that lets the user add a new record in memory, and a viewSummary() that calculates the total of all transactions. Moved the calculateTotal() function declaration into its own BudgetTools.h header file, implemented in BudgetTools.cpp.
