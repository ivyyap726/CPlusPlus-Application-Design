# Module 04 - Datasets, Arrays & Pointers

Loaded 5 real records (Date, Category, Amount) from the "Personal Finance Data" Kaggle dataset using a CSV file and fstream, stored them in parallel arrays, displayed them in a loop, and used a pointer to access the first amount.
